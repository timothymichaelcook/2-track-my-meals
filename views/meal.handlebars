<div class="text-center">
  <h2>{{meal_name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date meal_date}}</p>
  <p>
    <span for="img" aria-label="bowl">🥣</span>
    <span class="meals eated">{{ numberOftakeOut}}</span>
    eaten out!
  </p>
  <p>
    <button class="btn btn-primary">Track My Meals!</button>
  </p>
</div>
